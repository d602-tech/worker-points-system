# 115年度綜合施工處職安環保協助員系統 — 維護更新日誌

## 📅 2026-05-05 更新記錄 (v3.2.7 -> v3.3.0)

### 1. 部門主管介面 (DeptMgr Interface) 全面簡化與優化
*   **[優化] 導覽權限精簡**：移除主管不需使用的「人員管理」等行政功能，導覽列僅保留「差勤管理」、「績效評核」與「工作量彙總」。
*   **[新增] 差勤管理增強**：
    *   **國定假日標記**：整合年度國定假日設定，日曆預設標示週六日與節假日為非工作日。
    *   **特休統計列表**：於日曆下方新增統計資訊，動態呈現成員年度特休總額、累計已休與剩餘假額。
    *   **狀態篩選**：支援依特定假別（如：特、病、事）篩選日曆顯示內容。
*   **[重構] 績效評核模式**：
    *   **直接填報視圖**：由「審核制」改為「直接填報制」，主管可直接為全員輸入臨時點數並選擇績效等級 (優/佳/平)，系統自動換算對應點數。
    *   **即時同步**：簡化上傳流程，一鍵批次同步至 Google Sheet。
*   **[重構] 工作量彙總表 (個人月報視圖)**：
    *   **月報格式化**：針對主管角色將原本的彙總表格轉換為標準「個人工作月報表」格式，詳列每日點數細節。
    *   **資訊去敏感化**：移除所有新台幣金額與合約費用欄位，專注於點數與工作量。
*   **[新增] 全域操作防呆**：
    *   **強制確認視窗**：為差勤編輯、績效評核上傳等所有資料修改操作增加美觀的確認對話框 (Confirm Dialog)，防止誤操作。

---

## 📅 2026-05-04 Hotfix (v3.2.6 -> v3.2.7)

### MonthlyReport — ReferenceError: pointsConfig is not defined
*   **[緊急修正] Empty state 條件殘留舊變數**：v3.2.6 移除 `pointsConfig` state 時，第 857 行的 Empty state 渲染條件 `pointsConfig.length > 0` 未同步更新，導致頁面渲染時拋出 `ReferenceError: pointsConfig is not defined`。改為使用已計算的 `monthlyItemDefs.length > 0` 替代，語義相同且正確。

---

## 📅 2026-05-04 更新記錄 (v3.2.5 -> v3.2.6)

### 1. 日曆總覽 (CalendarOverview) — 偶發性 TypeError 修復
*   **[修正] `startsWith` of undefined 錯誤**：`deriveDayStatus` 函式中，當後端回傳的 `AttendanceRow.amStatus` 或 `pmStatus` 為 `undefined`/`null` 時，直接呼叫 `.startsWith()` 會拋出 TypeError。新增 `String(value ?? "")` 的 null-safe 轉型，確保任何資料狀態下均不崩潰。

### 2. 月報填報 (MonthlyReport) — 初始載入閃爍修復
*   **[修正] 先閃爍「尚無可填報項目」約 2 秒**：根因為 `POINTS_CONFIG_SEED` 使用動態 `import()` 載入，初始值為空陣列，導致載入 useEffect 在 `monthlyItemDefs = []` 時提早觸發一次，API 回傳後設為空 items 並關閉 loading，畫面閃爍「無項目」訊息。改為與 `CalendarOverview` 相同的**靜態 `import`**，消除三段非同步 race condition，載入流程從三段縮為一段，閃爍完全消失。

---

## 📅 2026-04-30 更新記錄 (v3.2.x -> v3.2.5)


### 1. 差勤排班模組 (AttendanceSchedule)
*   **[修正] 調班持久化失敗**：修復調班狀態重新整理後消失問題，實作 `amStatus="調"` 編碼與自動還原。
*   **[修正] 差勤日期顯示偏移 (提前一天)**：解決 GAS JSON 序列化導致的 UTC 時區偏移，統一改為後端格式化字串。
*   **[修正] 程式碼引用錯誤**：補齊 `date-fns/addDays` 引用。
*   **[新增] 排班天數上限驗證**：防止排定上班天數超過行政院標準天數。
*   **[優化] 請假時數自動計算**：預設 08:00 起始並自動扣除午休時段計算結束時間。
*   **[修正] 載入資料崩潰 (`rt.includes`)**：全面增加字串轉型檢查，防止非字串資料導致 JS 報錯。

### 2. 日曆與總覽 (CalendarOverview)
*   **[新增] 當月未完成項目面板**：動態追蹤未填報出勤、未傳佐證等漏項。
*   **[優化] 點數預估邏輯**：排除合約生效前的日期計算。

### 3. 月報填報 (MonthlyReport)
*   **[優化] 載入體驗**：增加詳細說明文字與骨架屏 (Skeleton)，修復進入頁面時「尚無項目」的短暫快閃現象。

### 4. 業務規則設定 (Contract Rules)
*   **[新增] 契約時間限制 (2026/04/22 起)**：定義 `CONTRACT_START` 常數，限制所有點數與排班功能僅於合約期間內生效。

---

## 📅 歷史更新回顧 (v1.0.0 -> v3.1.x)

### 1. 系統基礎與標準化 (Standardization)
*   **[優化] 術語統一 (Renaming Currency)**：將全系統貨幣單位「元」更名為「點」，同步更新 UI、Excel 匯出標題與後端欄位定義。
*   **[新增] 時間解析修復**：修正 Calendar 與 GAS 之間的時間戳記解析誤差，確保跨月份同步穩定。

### 2. 管理權限與安全性 (Security & Permissions)
*   **[新增] 類別限制**：限制 C 類別（績效類）項目僅能由管理端或特定權限人員填報，防止一般協助員自行操作。
*   **[優化] 資料去模擬化 (De-mocking)**：全面移除管理端（審核中心、報表摘要、配置頁）的 Mock Data，串接實際 Google Sheets API。

### 3. 數據統計與精準度 (Data Accuracy)
*   **[修正] 月度點數彙整邏輯**：重構 `MonthlyReport` 加總公式，確保每日任務、例行月報、績效加分能正確加總。
*   **[新增] 送出確認機制**：填報月報前顯示「確認摘要」，讓使用者核對本次填報之點數總額。

### 4. 行動端與操作體驗 (Mobile UX/UI)
*   **[優化] 行動端佈局**：修正罰單系統、動火證系統在手機上的「送出按鈕」遮擋問題。
*   **[新增] 滑動切換導航**：在填報頁面實作 Swipe 切換日期/月份，提升小螢幕操作效率。
*   **[優化] 檔案預覽功能**：支援在填報清單中直接查看已上傳之雲端硬碟檔案內容。

### 5. 違章管理系統 (Violation Management)
*   **[新增] 主辦/協辦連動**：根據選擇的違章扣款單位，自動連動對應的發單人清單。
*   **[優化] 儀表板預設值**：設定儀表板統計數據預設顯示為當月資料。

---

## 🛠️ 後續維護建議
1.  **Date 處理**：本系統嚴格依賴 `yyyy-MM-dd` 格式，修改日期邏輯時請務必使用 `date-fns` 並注意時區偏差。
2.  **API 權限**：所有新增的 `gasGet/gasPost` 調用必須維持 `callerEmail` 的權限檢查機制。
3.  **效能監控**：隨著 Google Sheets 資料量增加，建議定期執行試算表封存或快照以維持載入速度。

---
*記錄者：Antigravity AI (Coding Assistant)*
